# ── Production environment ─────────────────────────────────────────────────────
# Replace placeholder values with your real production endpoints before deploying.
PORT=4000
NODE_ENV=production
APP_ENV=production
CORS_ORIGINS=https://luxe.com,https://www.luxe.com

# Java REST API production endpoint
JAVA_API_URL=https://api.luxe.com
# JAVA_API_KEY is a secret — set it in the deployment environment, not here
