# ── Staging environment ────────────────────────────────────────────────────────
# Replace placeholder values with your real staging endpoints before deploying.
PORT=4000
NODE_ENV=production
APP_ENV=staging
CORS_ORIGINS=https://luxe.staging.com,https://studio.apollographql.com

# Java REST API staging endpoint
JAVA_API_URL=https://api.staging.luxe.com
# JAVA_API_KEY is a secret — set it in the deployment environment, not here
